// Top-level family group layout — no auth check here.
// Auth is enforced in the nested (portal) layout.
export default function FamilyGroupLayout({ children }: { children: React.ReactNode }) {
  return children
}
